
Clean up $lib
    Move all viewer things to $lib/module
    Move all util things to $lib/common

Improve VTF viewer
    VTF Environment map
    VTF Slices
    VTF Mipmaps
    
For some reason while transforming a viewer, it is called twice?
